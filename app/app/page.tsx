/**
 * Área do cliente — placeholder da Fase 1.
 * Dashboard real (saldo, % da meta, próximo vencimento, elegíveis) entra na Fase 3.
 */
export default function AreaClientePage() {
  return (
    <main className="mx-auto max-w-md px-4 py-10">
      <h1 className="text-xl font-semibold">Área do cliente</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Em construção. As telas do cliente serão implementadas na Fase 3.
      </p>
    </main>
  );
}
