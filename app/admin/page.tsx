/**
 * Painel administrativo — placeholder da Fase 1.
 * Dashboard real (caixa da compra programada, aportes do mês, clientes ativos,
 * veículos parados) entra na Fase 4.
 */
export default function PainelAdminPage() {
  return (
    <main className="mx-auto max-w-5xl px-6 py-10">
      <h1 className="text-2xl font-semibold">Painel administrativo</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Em construção. O CRUD de estoque, clientes e aportes entra na Fase 4.
      </p>
    </main>
  );
}
